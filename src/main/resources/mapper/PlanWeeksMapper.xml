<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.linfeng.plan.mapper.PlanWeeksMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.github.linfeng.plan.entity.PlanWeeks">
        <id column="week_id" property="weekId" />
        <result column="user_id" property="userId" />
        <result column="title" property="title" />
        <result column="remarks" property="remarks" />
        <result column="begin_date" property="beginDate" />
        <result column="end_date" property="endDate" />
        <result column="create_date" property="createDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        week_id, user_id, title, remarks, begin_date, end_date, create_date
    </sql>
  <select id="list" resultType="com.github.linfeng.plan.entity.PlanWeeks">
      select * from spwx_plan_weeks;
  </select>
    <select id="getById" resultType="com.github.linfeng.plan.entity.PlanWeeks">
        select * from spwx_plan_weeks where id=#{id};
    </select>

</mapper>
