name: "CodeQL"

on:
  push:
    branches: [ "main","dev" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "main","dev" ]
  schedule:
    - cron: '17 7 * * 0'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: java
            build-mode: none
          - language: javascript-typescript
            build-mode: none

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # Initializes the CodeQL tools for scanning.
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        queries: security-and-quality

    # ================= Java 构建步骤 =================
    - name: Setup Java JDK 1.8
      if: ${{ matrix.language == 'java' }}
      uses: actions/setup-java@v4
      with:
        java-version: '8'
        distribution: 'temurin'
        cache: maven

    - name: Build Maven Project (in serve/)
      if: ${{ matrix.language == 'java' }}
      run: mvn -B compile --file serve/pom.xml
      # 关键点：使用 --file 参数指定非根目录的 pom.xml 路径
      # CodeQL 会自动识别编译后的类路径

    # ================= JavaScript 构建步骤 =================
    - name: Setup Node.js
      if: ${{ matrix.language == 'javascript' }}
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        # 关键点：指定 package.json 所在的具体子目录
        cache-dependency-path: client/web/package.json

    - name: Install JS Dependencies
      if: ${{ matrix.language == 'javascript' }}
      run: npm install
      working-directory: client/web
      # 关键点：使用 working-directory 在特定目录下执行命令

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"
